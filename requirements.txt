# Python pip dependencies (conda env `dc`)
#
# conda-forge 로 먼저 설치:
#   numpy, trimesh, open3d, pythonocc-core
#
# 그다음 (Project 루트에서):
#   conda run -n dc pip install -r requirements.txt
msgpack>=1.0.0
tqdm>=4.0.0
occwl
